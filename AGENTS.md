# 唯一网站项目

遵守 `D:\Website\AGENTS.md`。当前目录 `D:\Website\site` 是唯一的液态玻璃最新版，所有功能修改、构建和部署均以这里为准。禁止沿用旧对话中的其他项目路径或复制出另一套项目。保留现有功能、内容及部署身份。

用户偏好极简结构：仅保留正在使用的组件与依赖，复用稳定的共用布局；不得为了精简而改动内容、样式、交互或路由。更改框架后运行 `npm run check` 和构建，并检查既有页面。

## 已停用的旧预览

旧的 Sites「zhangboyang · 玻璃融合预览」已从本地项目解绑。不要恢复 .openai/hosting.json、调用旧 Sites 项目或把旧卡片作为当前网站入口。唯一源码目录仍为 D:\Website\site，当前线上入口为 1014520.xyz。后续修改使用本目录和本地预览；部署须按用户授权执行。云端历史项目的删除状态未经确认，不得宣称已删除。

## 日记内容的选择范围

- 用户此次选择的依据是最初编号表格中露出的标题或原文开头。每个选中编号只导入该预览对应的完整独立内容及其配图；即使它很短，也不得改选同日更长的其他内容。
- 日期不代表文章边界。同日无关主题、短句、截图不能连带导入，也不能拆成额外获准文章。文章内部的经过、例子、论据和复盘应完整保留，不按段落或长度机械截断。
- 原编号、原始预览和最终文章须逐项建立可核对的映射。其他主题写入导入排除记录，避免再次误加。知识库始终只读。
