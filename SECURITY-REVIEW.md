# 安全检查 · 2026-09-17

## 结论

本轮发现并修复构建工具依赖链中的 4 条高危告警；它们来自同一个 sharp/libheif 公告及其上游依赖传播。修复后 npm audit 报告为 0。未在本轮覆盖范围中发现完整明文凭证或可执行的恶意文章内容。线上缺少若干安全响应头，仍有加固空间。

本轮基础提交：`3fb0b2fb5b6912958ae9abf722696e99b70db654`。检查对象是公开 GitHub 仓库的 3 次提交、当前网站源码与 public 资源，以及 `https://1014520.xyz` 的少量只读请求。未读取或修改原始知识库。

## 已修复

- `@cloudflare/vite-plugin`：1.54.5 → 1.54.11。
- `wrangler`：4.129.1 → 4.133.0。
- 配套 `@cloudflare/workers-types`：5.20260908.1 → 5.20260917.1。
- 上述升级更新了受影响的 miniflare/sharp 依赖；完整 npm audit 从 4 条 high 降至 0。
- GitHub 密钥扫描及推送保护已启用；启用后查询到的未关闭密钥告警为 0。该结果是查询时的状态，不保证所有未来扫描均为零。

漏洞涉及处理不受信任图片时的 libheif 内存问题，受影响链位于本项目的开发、构建工具依赖。本站没有自建图片上传接口，不能据此认定线上站点已被利用。[公告 GHSA-rgj7-g3m4-5g8c](https://github.com/advisories/GHSA-rgj7-g3m4-5g8c)

## 检查覆盖与结果

| 检查项 | 结果 |
| --- | --- |
| 公开历史 | 扫描 3 次提交中的 298 个不同文本对象；常见服务密钥、私钥、JWT、带凭证 URL 和赋值型凭证规则无命中 |
| 文章 HTML | 检查 80 段生成 HTML；未发现 script/iframe/object/embed/form、事件属性或危险协议链接 |
| SVG | 未发现脚本、事件属性、javascript 链接或 foreignObject |
| 图片元数据 | 360 张栅格图片均可解码；未发现 EXIF、XMP、注释等被检查的附加元数据 |
| 图片文字识别 | 图片和动图共 1,250 帧，去重后 1,240 帧全部完成本地识别；另对唯一 3.33 秒视频抽查 8 帧，共 1,248 个识别任务，无识别执行错误 |
| 图片候选复核 | 11 个关键词命中涉及 API 配置教程、平台缩略密钥、空白/隐藏密码输入或配置说明；未确认完整可用凭证。数字连写候选为配置哈希误报；截图中的 AppSecret 遮挡仍存在 |
| 图片分块 | 四个板块的媒体清单无缺失分块、无未引用分块；360 个媒体条目构建校验通过 |
| 返回链接 | 13 项本地用例通过；外站、协议相对地址、反斜杠、错误板块/锚点等输入回退到本地入口 |
| 线上访问 | 首页正常；`.env`、`.git/config`、`package.json`、导入清单和构建服务配置测试路径均返回 404；外站 from 参数未生成外站返回链接 |
| 构建验证 | `npm run check` 和 `npm run build` 均通过 |

## 待加固与限制

1. 首页响应尚未包含 Content-Security-Policy、X-Content-Type-Options、Strict-Transport-Security、X-Frame-Options 和 Referrer-Policy 头。页面已有 no-referrer 元标签，但不能替代其他响应头。建议部署层补充类型嗅探保护、页面嵌入限制和内容安全策略；内容安全策略须结合现有内联主题脚本验证，避免影响显示。
2. 本次仅修复本地与仓库的构建依赖并完成审计，没有重新部署线上网站，也没有把上述响应头标为已修复。
3. GitHub Dependabot 告警接口返回 403，未取得其告警清单；依赖结论来自本地 npm audit 的最新查询。
4. 图片识别可能漏掉小字、特殊排版或非文字凭证；视频是抽样检查。历史二进制文件未逐帧重新审查，历史文本已扫描。未进行攻击载荷、拒绝服务、压力测试或完整渗透测试。
5. 文章、生活记录、公开联系邮箱、账户标识及截图中的部分用户名仍属于作者授权发布的内容；本次没有重写或删改正文。公开信息并不等于匿名信息。

本地详细证据保存在 `D:\Website\work\security-20260917`，识别原文不提交仓库。以下保留旧检查记录，其中平台访问状态与依赖结果仅反映当时情况。

---
# 安全与隐私检查 · 2026-09-08

> 状态更新（2026-09-17）：作者已明确授权将 GitHub 仓库改为公开并更名为 `1014520.xyz`。以下为 2026-09-08 的历史检查记录，其中私有仓库与旧 Sites 访问控制的描述不代表当前状态。当前网站入口为 https://1014520.xyz。

## 检查结果

- 依赖检查原有 11 项警告（8 高、2 中、1 低），升级兼容组件后为 0。完整构建和 TypeScript 类型检查通过。
- 检查当前跟踪的文字文件、生成文章、构建目录和图片。凭证规则未发现完整明文密钥或私钥；导入的图片仍保留之前对 4 张截图、7 处凭证的精确遮挡。
- 本地 Optical Character Recognition（OCR，光学字符识别）覆盖 235 个图片文件，其中 10 个动图逐帧检查，共 1,125 帧。对疑似凭证项做了人工复核；部分截图里的密钥只是平台已缩略的显示值，配置输入框为空。截图中的主机提示符和软件包名称也有识别误报。
- 清除 10 张图片的拍摄信息、编辑信息或注释等附加元数据；逐像素比较确认画面与动图帧未改变。没有发现卫星定位字段。
- 文章生成内容没有脚本、事件属性、嵌入对象或外部图片地址。返回链接只允许原站首页或对应模块，拒绝外站来源。
- 网站没有自建登录、上传、评论或服务器写入接口。线上访问由 Sites 平台控制；检查时仅作者账户获准访问，无外部访客或共享组。无登录凭证请求首页、Explore、日常图片和敏感文件路径均返回 403。

## 已采取的措施

- 升级 React、vinext、Vite 及托管开发组件，修复依赖公告涉及的问题。
- 清除仓库副本中已从网页排除的博客末尾章节；原始知识库不改动。
- 导入脚本显式接收原稿目录，移除本机默认绝对路径；后续导入自动清理图片附加元数据。
- 排除本地缓存、环境文件、凭证文件和图片文字识别结果。
- 添加不索引和不发送来源信息的页面声明。它们只是隐私辅助设置，不能替代登录访问控制。
- GitHub 仓库从当前检查后的内容建立，不包含此前 112 次提交中的旧内容或已删除记录。

## 适用范围与限制

本次为源码、依赖、资源与访问边界检查，不是完整渗透测试，也不能保证不存在未知漏洞或图片识别漏检。没有进行漏洞利用、压力测试或浏览器交互回归。

页面仍保留作者主动选入的文章、生活记录、观点、联系邮箱与 GitHub 账号。仓库应维持私有。脱离 Sites 部署时，需要自行建立访问控制，原站的“仅作者可见”不会随源码迁移。

原始知识库及本地检查工作目录未纳入 GitHub。对于过去曾在其他地方分享过的明文凭证，图片遮挡不能使旧副本中的凭证失效；如仍在使用，应到对应服务重置。

参考公告：[React 服务端函数问题](https://github.com/advisories/GHSA-wx67-qw84-cm4g)、[Vite 开发服务器访问限制绕过](https://github.com/advisories/GHSA-fx2h-pf6j-xcff)、[图片尺寸解析问题](https://github.com/advisories/GHSA-w3rx-r6r6-pgpr)。
